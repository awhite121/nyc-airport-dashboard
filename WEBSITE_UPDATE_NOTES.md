# Taxi Project Website Update Notes

Target page:

```text
https://www.andrewwhitedata.com/taxi-travel-time-prediction
```

Use the file:

```text
taxi_project_page_updated.html
```

Recommended changes:

- Keep the slug as `/taxi-travel-time-prediction`.
- Replace the old embed/custom HTML block with the updated file.
- GitHub button points to:

```text
https://github.com/awhite121/NYC-Taxi-Trip-Duration-Late-Risk-Prediction
```

Data counts were corrected to match the uploaded data package:

- Raw rows: 3,574,091
- Clean modeling rows: 43,079

The older page mentioned ~50K clean trips and ~886K raw rows. The uploaded clean file contains 43,079 rows and the uploaded raw extract contains 3,574,091 rows, so the updated page uses the grounded counts.
